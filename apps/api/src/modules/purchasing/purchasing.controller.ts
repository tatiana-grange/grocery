/**
 * Admin-only purchasing routes. Holds more than one controller class (per the
 * `catalog.controller.ts` precedent): one nested under `/admin/suppliers/:supplierId`, one
 * under `/admin/purchasing`. Populated per user story.
 */
export {}

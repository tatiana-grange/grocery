import { Module } from '@nestjs/common'

/**
 * Lot 3 inventory: `StockMovement`, the append-only ledger a product's stock level and
 * weighted average cost price are derived from. Entities, contracts, and controller wiring
 * are added in the Foundational phase.
 */
@Module({})
export class InventoryModule {}

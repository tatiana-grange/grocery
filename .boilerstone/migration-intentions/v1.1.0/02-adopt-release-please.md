---
id: v1.1.0/adopt-release-please
domain: ci
classification: migration
requires:
  - v1.1.0/adopt-conventional-commits
---

# Adopt Release Please

## Goal

The project versions with release-please: a Release PR bumps the version and regenerates `CHANGELOG.md` from conventional squash commits, a short note in the docs app explains why that version exists, and a `v*` tag builds versioned GHCR images for every runnable app.

## Why

Commitlint alone is enough for an early project. Semantic versions should wait until two environments need different artifacts, or until production should move only when a human says so.

When that day comes, the version number, the changelog, and the tag must be derived from the squash commits this project already writes. Hand-editing `CHANGELOG.md` drifts from `git log`. release-please owns the math. A note under `apps/documentation/src/content/docs/releases/` is the human story of the bundle — that context is not in any single commit. Tag-triggered GHCR images let each environment pin a different artifact. The Promote workflow tells Dokploy which version to pull, from GitHub, so staging and production do not both follow `latest`.

Do not turn this on while the project still ships without versions. Do not force versioning on a project that is not ready.

## Applies When

- The project tracks the `ci` domain.
- A human has decided to turn on versioned releases (release-please, a human merges the Release PR).
- Any of these is missing: `release-please-config.json`, `.release-please-manifest.json`, `.github/workflows/release-please.yml`, a generated `CHANGELOG.md` header, `.github/workflows/release-note.yml`, `.github/workflows/promote.yml`, or `v*` / semver tags in `.github/workflows/push-to-ghcr.yml`.

## Do Not Apply When

- The project stays without versions (commitlint only, host builds from a branch). Record as skipped with that reason. This is the expected skip for early projects.
- A human has explicitly decided to keep their own release or versioning convention after reviewing this intention — record as skipped with that reason.

## Observable Gaps

Work through each gap independently; skip any that is already closed. Skip this whole intention when the project stays without versions.

1. **release-please config and workflow** — signal: no `release-please-config.json`, no `.release-please-manifest.json`, or no `.github/workflows/release-please.yml`.
   Adapt the staged reference `release-please-config.json` (`include-component-in-tag` must stay `false` so tags are `vX.Y.Z`; keep `always-update` true so the Release PR rebases when hidden commit types land on the default branch; keep `pull-request-title-pattern` as `chore(ci): release ${version}` so the title passes commitlint). Adapt `.release-please-manifest.json` so `"."` is this project's current version, not a guessed bump. Adapt `.github/workflows/release-please.yml` for the default branch if it is not `main`. Do not merge the Release PR with `GITHUB_TOKEN` — that merge never starts the follow-up run that creates the tag.
   Done when: the three files exist, the manifest version matches the project today, and the workflow opens or updates a Release PR on push to the default branch.

2. **Generated changelog** — signal: `CHANGELOG.md` is missing, or it is still a hand-edited Keep-a-Changelog file with an `[Unreleased]` section the team maintains by hand.
   Adapt the staged reference header (generated by release-please; do not edit by hand; rationale lives in commit bodies). Keep every existing version section the project already published. Do not invent new changelog lines.
   Done when: `CHANGELOG.md` keeps prior history and the top of the file states it is generated.

3. **Release notes and the versioning page** — signal: no `apps/documentation/src/content/docs/releases/index.mdx`, no `.github/workflows/release-note.yml`, no `apps/documentation/src/content/docs/references/1_release_and_versionning.mdx`, or the `project-release` skills are missing.
   Adapt the `releases/` collection: copy `index.mdx` if missing; keep any existing `v*.mdx` pages. Do not create a versioned note until a Release PR exists and the version is known. Copy `.github/workflows/release-note.yml` — the check is optional by default; the team sets the repository variable `REQUIRE_RELEASE_NOTE` to `true` when notes should block the Release PR. Copy `1_release_and_versionning.mdx`. Copy `.claude/skills/project-release/SKILL.md` and `.cursor/skills/project-release/SKILL.md`.
   Done when: the index, the workflow, the versioning page, and both skill files exist, and no unearned `releases/vX.Y.Z.mdx` was added.

4. **Tag-triggered GHCR images** — signal: `.github/workflows/push-to-ghcr.yml` has no `tags: ['v*']` trigger, or its metadata action has no `type=semver` tags.
   Adapt the staged reference workflow: add the `v*` trigger and the semver / `latest`-on-tag patterns. Keep the project's image matrix, Dockerfiles, and registry name. Include each remaining runnable app (API and frontends). Do not keep a frontend on branch build-on-deploy while the API uses versioned images.
   Done when: a push to the default branch still builds a SHA tag per existing Dockerfile, and a `v*` tag would produce `{{version}}`, `{{major}}.{{minor}}`, and `latest` for those images.

5. **Promote workflow** — signal: no `.github/workflows/promote.yml` or no `.github/workflows/promote-on-release.yml`.
   Copy the staged reference workflows. Do not invent extra git tags such as `env/prod`. Do not hook production to the `latest` image tag. Creating GitHub Environments `staging` and `production`, and storing Dokploy secrets, is a human step (`scripts/configure-github-repo.sh` and `scripts/github-repo-settings.md`). `promote-on-release.yml` is inert until a human sets the `PROMOTE_ON_RELEASE` repository variable; do not set it during this adaptation.
   Done when: `promote.yml` exists with `workflow_dispatch` inputs `environment` and `version`, jobs use GitHub Environments of those names, and `promote-on-release.yml` exists.

## Out of Scope

- commitlint, lefthook, `CONTRIBUTING.md`, PR lint, finalize-pr, and GitHub squash settings — those are `unreleased/adopt-conventional-commits`.
- The intention-gate workflow, `.boilerstone/migration-intentions/`, and the `boilerstone-intention` and `boilerstone-release` skills — producer only. Consumer projects do not write or promote intentions.
- Choosing or tagging a version by hand. Merging the Release PR. Creating a GitHub Release.
- Lockfiles (`pnpm-lock.yaml`) and generated artifacts.

## Reference Paths

- `release-please-config.json` — **adapt**
- `.release-please-manifest.json` — **adapt**
- `.github/workflows/release-please.yml` — **adapt**
- `.github/workflows/release-note.yml` — **copy**
- `CHANGELOG.md` — **adapt**
- `apps/documentation/src/content/docs/releases/` — **adapt**
- `.github/workflows/push-to-ghcr.yml` — **adapt**
- `.github/workflows/promote.yml` — **copy**
- `.github/workflows/promote-on-release.yml` — **copy**
- `apps/documentation/src/content/docs/references/1_release_and_versionning.mdx` — **copy**
- `.claude/skills/project-release/SKILL.md` — **copy**
- `.cursor/skills/project-release/SKILL.md` — **copy**

## Validation

- The three release-please files exist, `include-component-in-tag` is `false`, and `always-update` is `true`.
- `CHANGELOG.md` still contains the project's older version sections.
- `.github/workflows/release-note.yml` exists.
- `.github/workflows/push-to-ghcr.yml` lists a `v*` tag trigger and semver image tags.
- `.github/workflows/promote.yml` exists and is triggered by `workflow_dispatch`.
- `.github/workflows/promote-on-release.yml` exists and only dispatches when `PROMOTE_ON_RELEASE` is set.
- No new `apps/documentation/src/content/docs/releases/vX.Y.Z.mdx` was invented during this adaptation.

## Record Result

Run `pnpm boilerplate upgrade record --id unreleased/adopt-release-please --applied` after validation passes, or record it as skipped with a reason (staying without versions is a valid reason). The id stays `unreleased/…` until the boilerplate release promotes it.

# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
.next
out
build
test-results

# Playwright MCP scratch output (console logs, page snapshots)
.playwright-mcp/

# Expo / React Native
/android
/ios
/ios/Pods
.podfile.lock
.expo
.expo-shared

# Cache
.turbo
.cache
tsconfig.tsbuildinfo

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.docker

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
temp

.react-router

# Intermediate spec that `pnpm generate` fetches from a running API and feeds to
# openapi-ts. The generated client next to it IS tracked — the frontend imports it.
packages/openapi-generator/tmp/
openapi-ts-error-*

# android
android/

# ios
ios/

# Boilerplate upgrade workspace (temporary)
.boilerstone/upgrade/

# Local experiments / scratch notes
.lab/

# Personal local seed data (real contact info, machine-specific — never shared)
apps/api/src/seeders/*.local.seeder.ts

# Claude Code personal settings (the skills/ folder is shared and committed)
.claude/settings.local.json
.worktrees/
